<%= form_for(newdomain) do |f| %>
  <% if newdomain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(newdomain.errors.count, "error") %> prohibited this newdomain from being saved:</h2>

      <ul>
      <% newdomain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="form-group form-alignment">
    <label for="inputEmail1" class="col-lg-4 control-label">Domain Name</label>
    <div class="col-lg-8">
      <%= f.text_field :domain_name, class:"form-control", placeholder:"Domain Name" %>
    </div>
  </div>

  <%= f.hidden_field :rank, class:"form-control", placeholder:"Domain Name" %>
         
         
  <div class="col-lg-4 pull-right">
    <%= f.submit "Save", class:"btn btn-sm btn-primary btn-block" %>
  </div>
<% end %>
